<UserControl x:Class="EmployeeWorkTracker.Views.ReportsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
             xmlns:vm="clr-namespace:EmployeeWorkTracker.ViewModels">
    <UserControl.DataContext>
        <vm:ReportsViewModel/>
    </UserControl.DataContext>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0" Text="Отчёты по начислениям"
                   FontSize="24" FontWeight="Medium"
                   Foreground="#E8E8E8" Margin="32,24,32,16"/>

        <ScrollViewer Grid.Row="1" VerticalScrollBarVisibility="Auto" Padding="32,0">
            <ItemsControl ItemsSource="{Binding Payments}">
                <ItemsControl.ItemsPanel>
                    <ItemsPanelTemplate>
                        <StackPanel/>
                    </ItemsPanelTemplate>
                </ItemsControl.ItemsPanel>
                <ItemsControl.ItemTemplate>
                    <DataTemplate>
                        <Border Background="#252525" CornerRadius="12" Padding="24" Margin="0,0,0,16"
                                Cursor="Hand"
                                MouseLeftButtonUp="PaymentCard_MouseLeftButtonUp"
                                Tag="{Binding}">
                            <Grid>
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="Auto"/>
                                    <ColumnDefinition Width="*"/>
                                    <ColumnDefinition Width="Auto"/>
                                    <ColumnDefinition Width="Auto"/>
                                </Grid.ColumnDefinitions>

                                <Border Grid.Column="0" Width="80" Height="80" CornerRadius="40"
                                        Background="#1A1A1A" Margin="0,0,16,0"
                                        VerticalAlignment="Top">
                                    <Image Source="{Binding EmployeePhotoSource}" Stretch="UniformToFill">
                                        <Image.Clip>
                                            <EllipseGeometry Center="40,40" RadiusX="40" RadiusY="40"/>
                                        </Image.Clip>
                                    </Image>
                                </Border>

                                <StackPanel Grid.Column="1" VerticalAlignment="Center">
                                    <TextBlock Text="{Binding EmployeeName}" FontSize="16" FontWeight="Medium"
                                               Foreground="#E8E8E8" Margin="0,0,0,4"/>
                                    <TextBlock Text="{Binding PassportNumber}" FontSize="12"
                                               Foreground="#66BB9A" Margin="0,0,0,4"/>
                                    <TextBlock Text="{Binding PeriodDisplay}" FontSize="13"
                                               Foreground="#A0A0A0"/>
                                </StackPanel>

                                <Border Grid.Column="2" Width="100" Height="80" CornerRadius="8"
                                        Background="#1A1A1A" VerticalAlignment="Top" Margin="0,0,16,0">
                                    <Image Source="{Binding ReceiptPhotoSource}" Stretch="Uniform"/>
                                </Border>

                                <Button Grid.Column="3"
                                        Command="{Binding DataContext.DeletePaymentCommand, RelativeSource={RelativeSource AncestorType=UserControl}}"
                                        CommandParameter="{Binding}"
                                        Background="Transparent" BorderThickness="0"
                                        Padding="8" Cursor="Hand"
                                        ToolTip="Удалить начисление"
                                        VerticalAlignment="Top">
                                    <Button.Template>
                                        <ControlTemplate TargetType="Button">
                                            <Border x:Name="border" Background="Transparent" CornerRadius="8" Padding="{TemplateBinding Padding}">
                                                <materialDesign:PackIcon Kind="Delete" Width="18" Height="18" Foreground="#F2B8B5"/>
                                            </Border>
                                            <ControlTemplate.Triggers>
                                                <Trigger Property="IsMouseOver" Value="True">
                                                    <Setter TargetName="border" Property="Background" Value="#30F2B8B5"/>
                                                </Trigger>
                                            </ControlTemplate.Triggers>
                                        </ControlTemplate>
                                    </Button.Template>
                                </Button>
                            </Grid>
                        </Border>
                    </DataTemplate>
                </ItemsControl.ItemTemplate>
            </ItemsControl>
        </ScrollViewer>
    </Grid>
</UserControl>